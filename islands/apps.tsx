export default function Apps() {
  return (
    <section id="apps">
      <h2>Apps</h2>
      <p>Software download and management tool:</p>
      <pre>
        <code class="language-bash">
          {`# Nyanthu Package Manager (pkgm)
# The app requires the JVM.
# Supports macOS, Linux, Windows
curl https://www.nyanthu.com/pkgm.sh | sh`}
        </code>
      </pre>
      <p>
        <strong>Description:</strong> A tool for downloading and managing
        software packages easily across platforms.
      </p>
    </section>
  );
}
